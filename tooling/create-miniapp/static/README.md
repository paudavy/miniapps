Placeholder PWA icons for the create-miniapp CLI.

These files contain a minimal PNG encoded as base64 text so the CLI has a fallback when `assets/pwa` is missing in the consuming repo. They are placeholders — replace with proper PNG assets if you distribute this tooling.
